{
  "name": "nightwarden",
  "version": "2.65.0",
  "private": true,
  "engines": {
    "node": ">=24",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.5.1",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @nightwarden/api --filter @nightwarden/console --parallel dev",
    "dev:runner": "pnpm --filter @nightwarden/runner dev",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/dockerode": "4.0.1",
    "@types/node": "26.1.1",
    "esbuild": "0.28.1",
    "prettier": "3.9.5",
    "typescript": "7.0.2"
  }
}
