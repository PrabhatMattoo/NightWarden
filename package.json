{
  "name": "nightwatch",
  "version": "2.32.0",
  "private": true,
  "engines": {
    "node": ">=20",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.5.1",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev": "pnpm --filter './apps/**' --parallel dev",
    "dev:infra": "docker compose -f apps/runner/docker-compose.dev.yaml up -d"
  },
  "devDependencies": {
    "@types/dockerode": "^4.0.1",
    "@types/node": "^26.0.1",
    "prettier": "3.8.3",
    "typescript": "6.0.3"
  }
}
