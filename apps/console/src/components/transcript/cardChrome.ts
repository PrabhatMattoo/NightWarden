// Shared chrome for the transcript cards whose result IS a rendered artifact:
// the diff and the pull request. Everything else a tool produces is a row (see
// toolPresentation), so this is deliberately small.

export const TOOL_CARD_CLASS = "gap-3 border border-border py-3 ring-0";
