name: CI

on:
  push:
    branches: [main]
  pull_request:

# A superseded run on the same branch is answering a question nobody is asking.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    uses: ./.github/workflows/verify.yml
